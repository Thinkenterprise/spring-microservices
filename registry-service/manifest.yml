---
applications:
- name: registry-service
  host: registry-service-${random-word}
  memory: 512M
  instances: 1
  timeout: 90
  path: target/registry-service-0.0.1-SNAPSHOT.jar
  env:
    SPRING_PROFILES_ACTIVE: cloud
  services:
    - registry-service-database 
    - configuration-service
    - registry-service 